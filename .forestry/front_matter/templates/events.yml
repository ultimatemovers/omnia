---
label: events
hide_body: true
fields:
- name: layout
  type: text
  config:
    required: false
  label: layout
  default: events
  hidden: true
- name: main_img
  type: file
  config:
    maxSize: 64
  label: main_img
- name: events
  type: field_group_list
  fields:
  - name: title
    type: text
    config:
      required: false
    label: title
  - name: img
    type: file
    config:
      maxSize: 64
    label: img
  - name: text
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: true
      schema:
        format: html-blocks
    label: Text
  config:
    min: 
    max: 
    labelField: 
  label: events
pages:
- events.md
